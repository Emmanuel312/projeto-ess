import styled from 'styled-components'

export const Time = styled.input.attrs(props =>
({
    type: props.inputType,
}))
`
    font-family: 'PT Sans', sans-serif;
    font-size: 0.8rem;
    display: block;
    padding: 0.2rem 0.5rem;
    border-radius: 5px;
    width: 200px;
` 
